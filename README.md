# AI-Compiler-System
AI App Compiler is a compiler-inspired system that converts natural language requirements into structured application configurations. It uses a multi-stage pipeline for intent extraction, architecture planning, schema generation, validation, repair, and runtime simulation.
